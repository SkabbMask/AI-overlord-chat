# Model family: "claude" (default) or "gemini"
MODEL_FAMILY=claude

# Storage backend: "local" (default) or "convex"
STORAGE_BACKEND=local

# Required for Claude
ANTHROPIC_API_KEY=

# Required for Gemini
GEMINI_API_KEY=

# Required only when STORAGE_BACKEND=convex
CONVEX_URL=
